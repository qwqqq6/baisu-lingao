# 贡献指南

感谢你对《临高启明·执政者》感兴趣！本项目欢迎两类贡献：**代码**与**内容包**。内容贡献不需要会写代码。

## 环境搭建

只需要 Python 3.10+（无需 Node.js）：

```bash
git clone <仓库地址>
cd <仓库目录>/game
python serve.py 8080
```

浏览器访问 http://localhost:8080 即可游玩。改代码/内容后**普通刷新**即生效
（serve.py 已带禁用缓存响应头）。

提交前请运行内容校验：

```bash
python tools/validate_content.py
```

## 内容贡献（不写代码）

所有玩法内容都在 `game/content/official/` 下的 JSON 内容包里：

| 目录 | 内容 |
| --- | --- |
| `states/core.json` | 世界状态定义 |
| `forces/core.json` | 隐藏势力 |
| `series/core.json` | 事件系列 |
| `characters/core.json` | 人物（含继任权重、能力、结局表） |
| `cards/*.json` | 卡牌，按主题池分文件 |

### 卡牌贡献流程

1. 复制一张同类卡作骨架，修改 `id`（编号规则见下）、标题、正文与选项；
2. 运行 `python tools/validate_content.py`，修到零错误；
3. 提交 PR，附一句说明这张卡讲什么。

### 卡牌编号规则

`CORE-`（会议政治）、`FARM-`（农庄）、`CIV-`（民政）、`SEC-`（军务）、
`PLAN-`（计划）、`TRADE-`（商贸）、`EXT-`（外部）、`CHAR-XX-`（人物专属）。

## 对白写作规范（重要）

卡面是**对话**：谁在对首长说话、说了什么；选项是首长的口头批复。

- 全部使用白话口语，**禁止文言腔**（"伏乞批示"一类套话只允许出现在文书类事件）；
- 分角色语域：元老带"单位腔"，本地士绅说明代白话，归化民半生不熟，黎人疍家各有腔调；
- 执政者的批复选项是口语台词（"没错，就这么办。"），每张卡单独写；
- 人物专属卡可以复用名场面与口头禅（文总的锅、督工的表……）；
- **禁止粘贴小说原文**，引用设定与专有名词请注明出自《临高启明》。

详细规范见《docs/对白写作手册.md》与 `AGENTS.md`。

## 代码贡献

- 引擎是浏览器原生 ES Module（JS + JSDoc），运行时零依赖；
- 改 `game/src/` 后普通刷新即可看到效果；
- 大改动请先开 Issue 讨论；
- 提交信息用中文一句话说明动机。

## 架构速览

见 `AGENTS.md` 与 `docs/`。动手前请先读设计文档对应章节——实现必须与文档一致。
